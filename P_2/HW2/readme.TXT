I wrote two versions:
1.Hash.new 
2.Hash_2

Hash.new is better : cd Hash.new ;;;;;make;;;;;and run
both of them can work well,
you can use valgrind --tool=memcheck to check memory leak ,they are both no leak 